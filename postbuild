using IJulia
installkernel("Julia heap size limit", "--heap-size-hint=1G")

#!/bin/bash
set -e  # don't allow errors to fail silently
julia --project=. --threads=auto --startup-file=no --heap-size-hint=1G
using Pkg;
Pkg.add("PrettyTables")
quit()